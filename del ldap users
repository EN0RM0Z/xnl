SELECT 
    rc.CONSTRAINT_NAME AS fk_name,
    kcu.TABLE_NAME AS child_table,
    kcu.COLUMN_NAME AS foreign_key_column
FROM INFORMATION_SCHEMA.REFERENTIAL_CONSTRAINTS rc
JOIN INFORMATION_SCHEMA.KEY_COLUMN_USAGE kcu
    ON rc.CONSTRAINT_NAME = kcu.CONSTRAINT_NAME
    AND rc.CONSTRAINT_SCHEMA = kcu.CONSTRAINT_SCHEMA
WHERE rc.REFERENCED_TABLE_NAME = 'USER_ENTITY'
  AND rc.CONSTRAINT_SCHEMA = DATABASE();

-- 1. Остановите Keycloak перед выполнением.
-- 2. Сделайте резервную копию базы данных.
-- 3. Замените 'realm5' на имя вашего реалма.

-- Удаляем из таблиц, зависящих от USER_ENTITY
DELETE FROM FEDERATED_IDENTITY 
WHERE USER_ID IN (SELECT ID FROM USER_ENTITY WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL);

DELETE FROM USER_CONSENT 
WHERE USER_ID IN (SELECT ID FROM USER_ENTITY WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL);

DELETE FROM USER_ATTRIBUTE 
WHERE USER_ID IN (SELECT ID FROM USER_ENTITY WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL);

DELETE FROM CREDENTIAL 
WHERE USER_ID IN (SELECT ID FROM USER_ENTITY WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL);

DELETE FROM USER_GROUP_MEMBERSHIP 
WHERE USER_ID IN (SELECT ID FROM USER_ENTITY WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL);

DELETE FROM USER_REQUIRED_ACTION 
WHERE USER_ID IN (SELECT ID FROM USER_ENTITY WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL);

DELETE FROM USER_ROLE_MAPPING 
WHERE USER_ID IN (SELECT ID FROM USER_ENTITY WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL);

-- 4. Теперь удаляем самих пользователей
DELETE FROM USER_ENTITY 
WHERE REALM_ID = 'realm5' AND FEDERATION_LINK IS NOT NULL;

